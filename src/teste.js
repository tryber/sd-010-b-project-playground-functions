let parametro = ['Lucas','Cassiano','Ferraz','Paolillo'];
let result;

for (let index = 0; index < parametro.length; index+=1) {
    result = (parametro[parametro.length-1] + ', ' +parametro[0]);
  }
  console.log(result);